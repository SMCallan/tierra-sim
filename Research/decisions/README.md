# Research Decision Index

These records freeze material scientific and engineering choices before implementation. The
status in each record distinguishes specification, implementation, calibration, and formal
evidence; acceptance alone never implies empirical validation.

| ID | Decision | Primary effect |
|---|---|---|
| 0001 | Behavioural divergence is individual-first | Defines `p`, individual `delta`, population summaries, and run-level replication. |
| 0002 | Use one costly exploitative mechanism and omit `KILL` | Makes `EXEC_NBR` defensibly parasitic and avoids a predation confounder. |
| 0003 | Adopt a minimal research-oriented 16-opcode VM | Fixes the expressible mechanism set and computation semantics. |
| 0004 | Use bucketed trailing windows and explicit inactivity | Implements the MPR window without misclassifying inert organisms. |
| 0005 | Use run-level inference and mechanism controls | Retains the 48 assessed runs and adds 12 controls. |
| 0006 | Separate engine, runner, and visualisation | Makes headless runs deterministic, testable, and reproducible. |
| 0007 | Use one realised action for primary eligibility | Replaces a demographically incompatible threshold-five primary while retaining conservative sensitivities. |
| 0008 | Use deterministic cardinal donor search for `EXEC_NBR` | Removes an arbitrary one-direction encounter asymmetry while retaining local donor constraints. |
| 0009 | Use a spatial inoculum and reproduction economics before combat | Tests longer local interaction and slower host expansion without lineage-specific runtime privileges. |
| 0010 | Retain lineage-stratified failure and death diagnostics | Attributes every operation outcome and death cause without changing scientific state. |
| 0011 | Control `EXEC_NBR` donor compatibility before adoption | Compares exact addressing with deterministic donor `COPY` search without silently changing the production rule. |
| 0012 | Treat extinction as outcome and mark degenerate estimands | Replaces forced final coexistence with interaction exposure and prevents single-lineage information claims. |
| 0013 | Use donor-level `COPY` capability as the primary exploitation construct | Adopts cyclic donor search while retaining exact addressing as a defence sensitivity. |
| 0014 | Replace passive income with a local renewable-resource field | Introduces lineage-neutral spatial carrying capacity with exact resource–energy accounting. |
| 0015 | Continuous linear genome maintenance cost | Resolves D031 by eliminating 0-cost maintenance refuges and enforcing $L \ge 1 \implies \text{cost} \ge 1$. |
| 0016 | Shared neighbourhood resource depletion | Resolves D026 by allowing organisms to harvest from adjacent cells, restoring density-dependent carrying capacity. |
| 0017 | Correct the assessed research-gap statement | Restates the gap as an absent per-organism lineage-function disagreement measure after primary sources falsified the assessed lineage-label framing. |

Any later reversal receives a new decision record that identifies the superseded decision, rationale, and affected claims or datasets.
